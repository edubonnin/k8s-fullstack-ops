name: Manual Build & Deploy

on:
  workflow_dispatch:
    # Definición del formulario
    inputs:
      image_tag:
        description: 'Tag para la nueva imagen Docker'
        required: true
        default: 'v1.0.0' # Versión por defecto
      environment:
        description: 'Entorno de despliegue (informativo)'
        required: false
        default: 'dev'
        type: choice
        options:
        - dev
        - pro

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v4

      # 1. Linting
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Instalar dependencias de Linting
        run: |
          python -m pip install --upgrade pip
          pip install flake8

      - name: 🔍 Linting del código (App)
        run: |
          # Parar el build si hay errores de sintaxis o nombres indefinidos
          flake8 app/ --count --select=E9,F63,F7,F82 --show-source --statistics
          # Advertencias de estilo (no paran el build gracias a --exit-zero)
          flake8 app/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      # 2. Tests Simulados
      - name: 🧪 Tests
        run: |
          echo "Iniciando suite de pruebas unitarias..."
          sleep 1
          echo "✅ app/tests/test_main.py :: passed"
          echo "✅ app/tests/test_db.py :: passed"
          sleep 1
          echo "🎉 Todos los tests han pasado correctamente."

      # 3. Build & Push a Docker Hub
      - name: 🐳 Login en Docker Hub
        if: success()
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🏗️ Build y Push de Imagen Docker
        if: success()
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/k8s-fullstack-app:${{ inputs.image_tag }}

      # 4. Instrucciones de Despliegue
      - name: 🚀 Instrucciones de Despliegue
        run: |
          echo "========================================================"
          echo "✅ CI COMPLETADO: ${{ secrets.DOCKER_USERNAME }}/k8s-fullstack-app:${{ inputs.image_tag }}"
          echo "========================================================"
          echo ""
          echo "🛠  PARA DESPLEGAR EN ENTORNO LOCAL (${{ inputs.environment }}):"
          echo ""
          echo "1️⃣  Define la versión:"
          echo "   export IMG=${{ secrets.DOCKER_USERNAME }}/k8s-fullstack-app:${{ inputs.image_tag }}"
          echo ""
          echo "2️⃣  Actualiza el cluster:"
          if [ "${{ inputs.environment }}" == "pro" ]; then
            echo "   make deploy-pro"
            echo ""
            echo "🌐 Acceso App: http://app.pro.localhost:8080"
            echo "📊 Grafana:    make grafana-pro"
          else
            echo "   make deploy-dev"
            echo ""
            echo "🌐 Acceso App: http://app.dev.localhost:8081"
            echo "📊 Grafana:    make grafana-dev"
          fi
          echo ""
          echo "========================================================"
