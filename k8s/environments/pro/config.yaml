apiVersion: v1
kind: ConfigMap
metadata:
  name: infra-config
data:
  DB_HOST: "postgres-service"
  DB_NAME: "appdb"
  ENV: "pro"
  REDIS_HOST: "redis-service"
  MINIO_ENDPOINT: "minio-service:9000"
  MINIO_BUCKET: "assets"
