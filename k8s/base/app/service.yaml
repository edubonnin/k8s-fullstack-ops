apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: app
spec:
  type: NodePort
  selector:
    app: app
  ports:
    - name: http
      port: 80 # Puerto del servicio
      targetPort: 5000 # A donde se redirige el tráfico (hacia el container)
